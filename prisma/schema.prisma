generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("EXTERNAL_URL")
}


model products{
  id          Int     @id @default(autoincrement())
  title       String
  description String?
}